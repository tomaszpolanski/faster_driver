name: example
description: An example project

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  test: ^1.16.5
  bitsdojo_window: ^0.1.0
  http: ^0.13.1

flutter:
  uses-material-design: true